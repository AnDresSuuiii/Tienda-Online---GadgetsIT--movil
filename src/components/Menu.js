import React from "react";
import { StyleSheet, View, TextInput, ScrollView } from "react-native";
import { FontAwesome } from '@expo/vector-icons';

const Menu = ({ icon, placeholder }) => {
    return (
        <View >
          <ScrollView>
            
          </ScrollView>
        </View>
    );
};


export default Menu;
